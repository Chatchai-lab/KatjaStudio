package de.kataia.app.ui.theme

import androidx.compose.ui.graphics.Color

// Unsere warme Hauptfarbe (ein sanftes Pfirsich/Rosa)
val PrimaryWarm = Color(0xFFFFDAB9)
val PrimaryDark = Color(0xFFE6A38E)

// Sekundärfarben (ein beruhigendes Salbei-Grün)
val SecondarySage = Color(0xFFB2AC88)

// Hintergrundfarben (fast weiß, aber warm)
val BackgroundWarm = Color(0xFFFFF8F0)
val SurfaceWarm = Color(0xFFFFFCF7)

// Textfarben (kein hartes Schwarz, sondern ein dunkles Anthrazit-Braun)
val OnBackgroundDark = Color(0xFF2D2926)